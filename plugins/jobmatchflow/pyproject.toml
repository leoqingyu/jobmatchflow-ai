[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "jobmatchflow-agent"
version = "0.5.1"
description = "Local MCP bridge for JobMatchFlow application sessions"
requires-python = ">=3.10"
dependencies = [
  "mcp>=2.1,<3",
  "python-docx>=1.1,<2",
  "requests>=2.32,<3",
]

[project.scripts]
jobmatchflow-mcp = "jobmatchflow_mcp_server:main"

[tool.setuptools]
py-modules = ["jobmatchflow_mcp_server"]
package-dir = {"" = "scripts"}
