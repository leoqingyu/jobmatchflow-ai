interface:
  display_name: "JobMatchFlow Interview"
  short_description: "Prepare from the exact submitted application"
  brand_color: "#2563EB"
  default_prompt: "Use $jobmatchflow-interview to prepare me locally for one tracked interview from the exact application context and append only a concise durable note to JobMatchFlow."

dependencies:
  tools:
    - type: "mcp"
      value: "jobmatchflow"
      description: "JobMatchFlow application snapshots, submitted materials, and append-only application notes"

policy:
  allow_implicit_invocation: false
