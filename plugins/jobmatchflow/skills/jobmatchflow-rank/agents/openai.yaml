interface:
  display_name: "JobMatchFlow Job Ranking"
  short_description: "Interview preferences, rank jobs, curate Preparing"
  brand_color: "#2563EB"
  default_prompt: "Use $jobmatchflow-rank to confirm or update my saved preferences, then rank my Job List and add qualified matches to Preparing."

dependencies:
  tools:
    - type: "mcp"
      value: "jobmatchflow"
      description: "JobMatchFlow Experience, Job List, full JD, match evidence, scores, and Preparing actions"

policy:
  allow_implicit_invocation: false
