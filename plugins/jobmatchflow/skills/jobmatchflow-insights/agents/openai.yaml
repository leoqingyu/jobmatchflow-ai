interface:
  display_name: "JobMatchFlow Insights"
  short_description: "Analyze application outcomes and funnel patterns"
  brand_color: "#2563EB"
  default_prompt: "Use $jobmatchflow-insights to analyze my existing JobMatchFlow application funnel and propose evidence-based targeting experiments."

dependencies:
  tools:
    - type: "mcp"
      value: "jobmatchflow"
      description: "JobMatchFlow jobs, tracking, scores, stages, and available feedback"

policy:
  allow_implicit_invocation: false
